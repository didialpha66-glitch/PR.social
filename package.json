{
  "name": "prsocial-bot-app",
  "version": "1.0.0",
  "description": "PR Social Bot APK with Background Execution Support",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "cordova-plugin-background-mode": "^0.9.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
